{
  "name": "movieapidemo",
  "version": "1.0.0",
  "description": "Proyecto de demo API en Node, llamadas a api externa",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "lint:watch": "onchange \"src/**/*.js\" -- npm run lint --silent",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "node",
    "express",
    "require"
  ],
  "author": "Eduardo Tobar Peralta",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^6.2.2"
  },
  "dependencies": {
    "express": "^4.17.3",
    "request": "^2.88.0"
  }
}
